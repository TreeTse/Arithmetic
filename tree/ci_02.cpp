/*
struct TreeNode {
	int val;
	struct TreeNode *left;
	struct TreeNode *right;
	TreeNode(int x) :
			val(x), left(NULL), right(NULL) {
	}
};*/
class Solution {
public:
    int TreeDepth(TreeNode* pRoot)
    {
        TreeNode* pSrc = pRoot;
        if(pSrc == NULL){
            if(count >= deplong)
                return 
        }
    }
private:
    int count = 0;
    int deplong = 0;
};